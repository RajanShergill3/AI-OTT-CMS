# Validators

Request validation schemas (Zod or Joi) for body, query, and params.

Planned validators:

- `auth.validator.ts`
- `movie.validator.ts`
- `category.validator.ts`
- `user.validator.ts`
- `common.validator.ts`
